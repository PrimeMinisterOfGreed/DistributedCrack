\section{Attacco con ricerca lineare}
Un attacco alle password, è abbastanza simile almeno in forma teorica, a un algoritmo di ricerca lineare di un elemento conosciuto in una lista. Infatti ,almeno nella sua forma più semplice , si può pensare al database come ad una lista di coppie utente-password, in cui si vuole cercare la presenza di un elemento conosciuto, cioè la password che correntemente si sta cercando di testare. Si supponga di avere N coppie utente-password e K password da testare, se si implementasse un algoritmo lineare sequenziale, il tempo impiegato per lo stesso per trovare la password sarebbe $N*K$, lo stesso in termini di spazio occupato, entrambi questi costi possono essere utilizzati come caso base per stimare un possibile speed up. 

\subsection{Ricerca Lineare con architettura main worker}
Dovendo effettuare una comparazione, si può distribuire il file con le password a tutti i nodi e dividere in P parti, quanti sono gli esecutori, il file del database. A questo punto idealmente  il file del database viene distribuito in modo statico, o in modo dinamico. Nel primo caso significa che all'inizio verranno trasmessi $(P*N)/P=N$ bytes per distribuire il database e $P*K$ per distribuire le password da testare, per poi cominciare ad eseguire gli $(N/P*K)$ confronti, in termini di complessità non ci si è allontanati molto dal caso base purtroppo, ma la cosa peggiore di questo metodo è che all'inizio presuppone di saturare molto l'I/O dei vari nodi per scambiarsi il file senza fare nient'altro, che può comportare uno spreco di tempo non indifferente. Nel secondo caso, la situazione migliora già notevolmente, ipotizzando un architettura main-worker infatti, il main divide il file in pezzi equi spaziati che i worker richiedono ogni volta che finiscono la computazione su uno, bisogna comunque distribuire all'inizio le copie delle password da testare, per cui la complessità computazionale e lo spazio occupato non cambiano rispetto al caso precedente, ma almeno la trasmissione del database e il confronto avverranno parallelamente. 

\subsubsection{Ipotesi di implementazione}
L'implementazione del modello precendentemente discusso è piuttosto semplice almeno concettualmente. 
\begin{figure}[H]
    \includegraphics[width=0.6\textwidth]{images/main_worker_diagram.png}
    \caption{Architettura main worker proposta per il problema}
\end{figure}

Supponendo di aver completato la parte iniziale di scambio del file con le password da testare, rimane solo più il file del database, che può essere suddiviso e scambiato con i nodi su richiesta, sotto forma di Task. Ogni qualvolta il worker è vuoto chiede al nodo root un task, questo può essere o una coppia di indici, nel caso in cui ad esempio il file sia condiviso tramite clustered filesystem, oppure direttamente un array di stringhe qual'ora questo meccanismo non sia disponibile. Ogni qualvolta un worker trova una delle password nella porzione di file che gli è stata assegnata, questi lo notifica al nodo Root che rimane in attesa di questa tipologia di messaggi, e registra il risultato che alla fine della computazione verrà mostrato all'utente.

\subsubsection{Speedup, efficienza e ccratio}
Usando il caso iniziale sequenziale $T_{seq} = N*K$, si può calcolare in via teorica lo speedup, nel caso si utilizzino P processori ai quali è stato trasmesso precedentemente il file con le password, il loro tempo di esecuzione sarà $T(P)=(N/P)*K$ a questo punto lo speedup assoluto sarà $S=T_{seq}/T(P)=\frac{N*K}{(N/P)*K}=\frac{N*P}{N}$, in termini di efficienza si ottiene $E=S/p= \frac{N*P}{N}*\frac{1}{P}=1$, per cui uno scaling ideale e un efficienza ideale. Purtroppo questa soluzione è poco praticabile con l'aumentare della quantità di dati, infatti è necessario trasmettere $K*P$ password per distribuire il file e $N/P$ pezzi di database in totale 


\subsection{Un architettura alternativa al main-worker}

Nel caso del main worker si è costretti a passare almeno uno dei due file in copie ai diversi nodi. A meno di non assicurarsi con metodi più o meno complicati, che ogni porzione del file di password sia stata comparata con ogni porzione del database in circolazione tra i nodi. In alternativa, si può adottare un'architettura a pipeline con topologia ad anello ($N \to N+1 \pmod{P}$). In questo modello, il dizionario delle password viene suddiviso tra i nodi, mentre i blocchi del database "scorrono" attraverso la pipeline. Ad ogni stadio, un blocco di dati viene confrontato con il set di password locale, realizzando un confronto completo dopo un intero ciclo della pipeline.

\subsubsection{Ipotesi di implementazione}

\begin{figure}[H]
    \includegraphics[width=0.6\textwidth]{images/pipeline_diagram.png}
    \caption{Architettura ad anello}
\end{figure}

In questa architettura, il primo passo è distribuire le password da testare ai vari nodi, dopodichè si comincia distribuendo il primo frammento del database al primo nodo, che comincierà il processamento e lo passerà al nodo successivo. In questo momento transitorio, viene a galla uno degli svantaggi di questa architettura rispetto alla precedente, ovvero l'esistenza di un periodo transitorio più o meno lungo nel quale alcuni nodi rimangono in attesa del task da compiere, allo stesso modo verso la fine della computazione, i nodi al termine della pipeline avranno dei task da finire mentre i primi sono ormai fermi. Uno degli obiettivi è quindi quello di diminuire il più possibile questo periodo transitorio, che in un certo senso può essere modellato come un tradeoff, infatti questo periodo dura proporzionalmente se si vuole alla dimensione dei singoli frammenti, infatti riducendo man mano la loro dimensione i task verranno completati più rapidamente, ma si introdurrà un carico maggiore sull'IO che dovrà smistare più messaggi.

